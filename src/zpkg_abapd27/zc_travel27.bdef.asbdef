projection;
strict ( 2 );
use draft;

define behavior for ZC_TRAVEL27 alias Travel
{
  use create;
  use update;
  use delete;

  use action prepareTravel;
  use action bookTravel;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}